eureka:
  client:
    service-url:
      defaultZone: http://eurekaserver:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

spring:
  application:
    name: board
  profiles:
    active: prod
  config:
    import: configserver:http://config:8888/board/prod/master